"""Code generation tooling for controller variants.

Invoked via ``python -m mpc_acados_core.generate --controller <name>``. The
generator reads a ``model_definition.yaml`` living in a controller package
and writes its Python/C++ artifacts into that same controller.
"""
